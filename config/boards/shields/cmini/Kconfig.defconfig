if SHIELD_CMINI_LEFT

    config ZMK_KEYBOARD_NAME
        default "CMini(Left)"

endif

if SHIELD_CMINI_RIGHT

    config ZMK_KEYBOARD_NAME
        default "CMini(Right)"
    config ZMK_SPLIT_BLE_ROLE_CENTRAL
        default y

endif

if SHIELD_CMINI_LEFT || SHIELD_CMINI_RIGHT

    config ZMK_SPLIT
        default y

endif